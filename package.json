{
  "name": "play.flix.dev",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://play.flix.dev/",
  "dependencies": {
    "ace-builds": "^1.35.1",
    "lz-string": "^1.5.0",
    "pretty-ms": "^8.0.0",
    "reconnecting-websocket": "^4.4.0"
  },
  "scripts": {
    "build-script": "node build-scripts/main.mjs",
    "build": "npm run build-script && esbuild src/index.js --bundle --minify --outfile=public/build/bundle.js",
    "watch": "npm run build -- --sourcemap --watch",
    "start": "npm run watch -- --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
